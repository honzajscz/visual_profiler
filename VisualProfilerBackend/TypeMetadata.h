#pragma once

#include "stdafx.h"
#include <cor.h>
#include <corprof.h>
#include <string>
#include <map>

using namespace std;

class TypeMetadata
{
public:
	//pIMetaDataImport->GetModuleFromScope
	
	wstring Namexxx;
	
	TypeMetadata(void);
	~TypeMetadata(void);

private:
	//map<
	//shared_ptr<AssemblyMetadata> _pDefiningAssemblyMetadata;
};

